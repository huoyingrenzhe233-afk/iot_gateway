# 跨平台换行符统一:仓库存 LF,Windows 检出自动转 CRLF
* text=auto

# shell 脚本强制 LF(Windows 上 CRLF 会导致 bash 报错)
*.sh text eol=lf

# Windows 批处理强制 CRLF
*.bat text eol=crlf

# 二进制文件不处理
*.png binary
*.jpg binary
*.ico binary
*.so binary
*.dll binary
*.exe binary
