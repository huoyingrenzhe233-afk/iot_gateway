# 开发日报 — 2026-08-12(周三)

> 分支:`feature/device`(已推送远端)
> 阶段进度:阶段一 ✅ 阶段二 ✅ 阶段三 ✅(本机全链路) 阶段四 ✅(设备状态管理)

## 一、今日成果总览

| 类别 | 内容 | 状态 |
|---|---|---|
| 阶段三收尾 | MQTT 客户端 + /api/control 全链路(本机真实 MQTT 闭环) | ✅ 本机验证 |
| 阶段四 | 设备状态管理 + 注册表 + /api/status + /api/devices | ✅ 本机验证 |
| 协议对齐 | /api/control 键名定稿 payload(对齐前端 index.html) | ✅ 实测 200 |
| 代码质量 | 7 项审查问题修复(含部分字段下发 bug) | ✅ 实测 |

## 二、功能开发

### 1. 设备状态管理(阶段四核心)

- **`src/core/device/device.{h,cpp}`**:6 外设状态缓存
  - `update_from_report`:解析 sensor 上报(4 传感器值)+ status 回执(items 数组,长短名兼容)
  - `update_from_control`:控制命令缓存(UI 秒响应,回执再校准)
  - `get_status_json`:生成 /api/status 的 10 字段聚合 JSON
- **`src/core/device/device_registry.{h,cpp}`**:静态登记表,读 config/devices/*.yaml
  - sensors.yaml(4 传感器:temp_1/humi_1/light_1/ir_1)+ actuators.yaml(3 执行器:led_1/motor_1/buzzer_1)
  - `json_escape` 转义 + `kind` 校验
- **main.cpp 集成**:/api/status + /api/devices 路由,on_message 挂接,fn_data 传参

### 2. MQTT 控制链全链路验证(强证据)

- 独立 `mosquitto_sub` 捕获到网关发布的完整 cmd 信封 ✅
- `mosquitto_pub` 模拟上报,网关 on_message 收到完整 sensor 信封 ✅
- 完整 API 测试:health/version/devices/status/control/404 全过 ✅

## 三、问题修复(代码审查驱动)

| # | 问题 | 修复 |
|---|---|---|
| 1 | status 回执解析为空(执行器状态永远 0) | 实现 items 数组遍历 + name 映射 |
| 2 | last_seen 带引号(tok vs str) | 改 mg_json_get_str |
| 3 | JSON 无转义(特殊字符破坏) | json_escape() |
| 4 | kind 无校验 | 只接受 sensor/actuator |
| 5 | 回执遍历 -1 越界(与合法值冲突) | 改 mg_json_get_tok 存在性判断 |
| 6 | **部分字段下发重置 bug(实测复现)** | update_if_present lambda + tok 判断 |
| 7 | **/api/control 键名 body/payload 不一致** | 统一 payload(对齐前端定稿) |

## 四、协议对齐(重要决策)

**/api/control 请求体键名定稿:`payload`**
- 前端 index.html 定稿用 `payload`(最初网关用 body,曾要求 Web 同事改,后确认前端是最终版)
- 实测:payload + 6 字段 → 200;旧 body 格式 → 400(键名切换生效)

**前端 ↔ 网关协议确认**:
- 前端控制:全量 6 字段一起发(`led_on/led_br/motor_on/motor_sp/motor_dir/buzzer`)
- 前端状态:1s 轮询 /api/status,渲染 4 传感器 + 执行器
- ⚠️ 前端已调用 5 个摄像头接口(/api/camera/*),**网关未实现,联调前必补**

## 五、git 提交记录(今日 5 个)

```
c90076b docs: HANDOFF 同步部分字段下发修复记录(D0)
32c9fc1 fix(gateway): 控制缓存支持部分字段下发 + 文档同步
538b374 fix(gateway): /api/control 键名改回 payload(对齐前端定稿)
0e4e0d1 docs: 新增代码解析教学文档
6583717 feat(gateway): 设备状态管理 + 注册表 + /api/status + /api/devices
```

另:合并了 PR #11(mqttclient)、PR #12(control)进 main。

## 六、待办(明天)

| 优先级 | 事项 |
|---|---|
| 🔴 | 规则引擎(20 分)——先定"id→MQTT 字段映射"(HANDOFF 待办) |
| 🔴 | 摄像头 5 接口(前端已调用,联调前必做) |
| 🟡 | 板上验证阶段三/四(本机已过,板上待做) |
| 🟡 | /api/devices/:id 单设备详情 |
| 🟡 | WebSocket /ws(实时推送) |
| 🟢 | feature/device 开 PR 合 main |

## 七、今日踩坑(答辩素材,详见代码解析文档第 9 节)

1. 两个进程同 client_id → MQTT 互踢(1 秒断连循环)
2. mg_str 字段是 .buf 不是 .ptr(版本坑)
3. JSON 数字 vs 字符串要选对解析函数(get_num / get_str)
4. 默认值 -1/0 和"字段缺失"语义混淆(双重语义 bug 温床)
5. 前端/网关键名不一致(payload vs body)——以最终前端定稿为准
