# CMake 构建产物
build/
build-arm/

# 编译数据库副本(由 CMake copy_compile_commands 目标自动生成,含本机绝对路径,不入库)
/compile_commands.json

# opencode 工具内部数据
.omo/

# 编译产物
*.o
*.a
*.so
gateway_arm

# 运行日志
*.log

# 摄像头运行期产物(抓拍照片/录像,由 CameraManager 生成)
snapshots/
records/

# 编辑器
.vscode/
.idea/
*.swp
.clangd

# Qt
*.pro.user
*.autosave
